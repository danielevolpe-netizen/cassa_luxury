# Neon Postgres pooled connection string
DATABASE_URL="postgresql://USER:PASSWORD@HOST-pooler.REGION.aws.neon.tech/DB?channel_binding=require&sslmode=require"

# Auth.js — genera con: npx auth secret
AUTH_SECRET=""

# Solo in sviluppo locale. In produzione (Vercel) NON serve: trustHost è attivo.
AUTH_URL="http://localhost:3000"
